export enum API_RESPONSE {
  OK = "ok",
  FAILED = "failed",
  CREATED = "created",
}

export enum STATUS_CODES {
  SUCCESS = 200,
  INTERNAL_SERVER_ERROR = 500,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  CREATED = 201,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
}
